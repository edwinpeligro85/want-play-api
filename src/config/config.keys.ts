export enum Config {
  PORT = 'port',
  PRODUCTION = 'production',
  DATABASE = 'database',
  JWT_SECRET = 'jwtSecret',
  API_PREFIX = 'apiPrefix',
  CLIENT_APP_URL = 'clientAppUrl',
  MAILER = 'mailer',
}
